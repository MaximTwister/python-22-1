# pixabay.com
#
#
# Necessary libraries: requests; threading
# *Addition libraries: PIL (install with pip), io.BytesIO
# Hint: `response.content`
#
#
# 0. Write `timeit` decorator
# 1. Function -> 50 (images) synchronous downloads.
# 2. Function -> 50 (images) in separate threads downloads.
# 3. Calculate time of execution with `timeit` decorator
